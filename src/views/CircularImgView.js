/* import React from 'react';
import { View, StyleSheet } from 'react-native';
import photo from '../assets/Engagement1.jpg'

const CircularImgView = () => {
  return (
    <View style={styles.container}>
      <img src={photo} alt="Photo" style={styles.circularImgView}></img>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  circularImgView: {
    width: 150,
    height: 150,
    borderRadius: 75, // Half of width and height to make it circular
  },
});

export default CircularImgView; */